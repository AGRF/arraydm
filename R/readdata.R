# Load data
#
# This script loads data files into R
#
# Some useful keyboard shortcuts for package authoring:
#
#   Build and Reload Package:  'Cmd + Shift + B'
#   Check Package:             'Cmd + Shift + E'
#   Test Package:              'Cmd + Shift + T'

readdata <- function(contractID, dir, pattern) {
  # CONTRACT="ILMLEPIC-15325"
  # TARGETS = read.metharray.sheet(base="/data/Bioinfo/bioinfo-proj-grayl/projects_methylation/CAGRF20422/", pattern=".csv$")
  # DATA = "IDAT"
  # EPIC = read.metharray.exp(base = DATA)
  }
